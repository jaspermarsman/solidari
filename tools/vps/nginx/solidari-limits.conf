# Solidari — rate limiting zones (http-context). PLAN-1 fase 1.5a / PLAN-4 fase 3.
limit_req_zone $binary_remote_addr zone=solidari_api:10m rate=6r/m;
limit_req_zone $binary_remote_addr zone=solidari_tts:10m rate=30r/m;
limit_req_status 429;
